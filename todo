* working on the actual game portion of it
	* who manages the assignments? A new game object?
	* who manages the round?
	* currently the game component is expecting more data from gameUpdate than is being provided
* chat update should happen regardless of whether game update happens
* once a game starts the players should be solidified and NOT removed from everything if they dc
	* a player should be able to rejoin after DC if they save their playerId
